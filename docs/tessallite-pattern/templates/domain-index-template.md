# <Domain Name> Index

Status: active
Last meaningful update: YYYY-MM-DD

This is the L1 index for `<domain>`. It summarizes the domain and lists every
active document in this domain. If the domain uses nested folders, list nested
documents by relative path, such as `prompts/example-prompt.md`.

## Current State

Write 3 to 8 lines describing the current state of this documentation domain.
Mention what is authoritative, what is in progress, and what readers should
open first.

## Active Documents

| File | Purpose | Status |
| --- | --- | --- |
| [`<domain>_<name>.md`](<domain>_<name>.md) | `<purpose>` | active |
| [`<nested>/<file>.md`](<nested>/<file>.md) | `<purpose>` | active |

## Draft Or Pending Documents

| File | Purpose | Status |
| --- | --- | --- |
| [`<domain>_<name>.md`](<domain>_<name>.md) | `<purpose>` | draft |

## Archived Or Superseded Documents

Move archived entries to the archive index if your project has one. If this
domain keeps local archive references, list them here.

| File | Reason |
| --- | --- |
| [`<file>`](<file>) | superseded/duplicate/closed |

## Routing Notes

- Start with `<file>` for `<need>`.
- Use `<file>` when investigating `<topic>`.
- If a nested folder has its own `_INDEX.md`, use it as the local inventory for
  that folder.
